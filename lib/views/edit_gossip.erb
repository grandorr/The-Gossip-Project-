<html>
  <head>
    <title>Modifier un potin</title>
  </head>
  <body>
    <h1>Modifie un potin </h1>
    <p>Potin numéro <%= id %>
    <p>Voici le potin actuel :</p>
    <% gossips.each do |gossip| %>
    <p> Le nom de l'auteur :<strong> <%= gossip.author%> </strong>
    <p> Le contenu du potin :<strong> <%= gossip.content%></strong> </p>
    <% end%>
    <form action='/gossips/edit/<%=id%>' method='POST'>
      Modifie l'autheur :   <input type='text' name='gossip_author'/><br/>
      Modifie le contenue : <input type='text' name='gossip_content'/><br/>
        <input type='submit'/> 
    <a href="/"><button type="button" class="btn btn-lg btn-block btn-secondary">Retour</button></a>
    </form>
  </body>
</html>